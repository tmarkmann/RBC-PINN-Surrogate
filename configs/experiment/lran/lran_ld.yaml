# @package _global_

data:
  data_dir: /vol/cfd/rbc/3D-ld
  start: 500
  end: 1000
  train_length: 12

model:
  ae_ckpt: ${paths.model_dir}/3d_ae/ld/ra${data.ra}-${seed}.ckpt
  latent_dimension: 1024
  train_autoencoder: true

tags:
  - train
  - ld