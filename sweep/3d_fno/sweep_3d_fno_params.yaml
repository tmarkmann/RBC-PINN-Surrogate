program: scripts/train_3d_fno.py
name: FNO_Params

method: grid
metric:
  goal: minimize
  name: val/loss

parameters:
  seed:
    values: [0, 1, 2]
  
  model.hidden_channels:
    values: [8, 16, 32, 40, 48]

command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}